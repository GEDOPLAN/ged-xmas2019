import { VisuellDragDirective } from './visuell-drag.directive';

describe('VisuellDragDirective', () => {
  it('should create an instance', () => {
    const directive = new VisuellDragDirective();
    expect(directive).toBeTruthy();
  });
});
